# Base de datos SQLite (se genera en cada servidor, no se versiona)
pfw03.db
pfw03.db-shm
pfw03.db-wal

# Logs de ejecución
*.out
*.log

# Certificados TLS: la clave privada NUNCA se versiona.
# Se generan en cada servidor (ver README, sección HTTPS).
certs/

# Node
node_modules/
